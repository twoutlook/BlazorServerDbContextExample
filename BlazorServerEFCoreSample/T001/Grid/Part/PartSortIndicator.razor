@inject IPartFilters Filters

@if (Column == Filters.SortColumn)
{
    <span>@SortSymbol</span>
}
else
{
    <span>&nbsp;</span>
}
@code {
    /// <summary>
    /// Sets which <see cref="PartFilterColumns"/> this indicator is for.
    /// </summary>
    [Parameter]
    public PartFilterColumns Column { get; set; }

    /// <summary>
    /// The symbol to render.
    /// </summary>
    private string SortSymbol => Filters.SortAscending ? "🔼" : "🔽";
}
