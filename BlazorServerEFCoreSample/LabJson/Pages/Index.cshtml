@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<ul>

    @foreach (var x in Model.items)
    {
        <li>
            @x.Entity

            <ul>
                @foreach (var x2 in x.Sortstr)
                {
                    <li>@x2</li>
                }
            </ul>
        </li>
        <hr />
    }


